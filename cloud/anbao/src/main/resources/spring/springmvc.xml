<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
        http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.2.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.2.xsd">

	<context:component-scan base-package="com.anbao.controllor" />
	<mvc:annotation-driven />

	<!-- 认证拦截器 -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/**"/>
			<mvc:exclude-mapping path="/user/login"/>
			<mvc:exclude-mapping path="/user/register"/>
			<mvc:exclude-mapping path="/user/getPhoneCode"/>
			<mvc:exclude-mapping path="/user/createPassword"/>
			<mvc:exclude-mapping path="/normalup/**"/>
			<mvc:exclude-mapping path="/upState/**"/>
			<mvc:exclude-mapping path="/uploadVideo/**"/>
			<mvc:exclude-mapping path="/addequipment/**"/>
			<mvc:exclude-mapping path="/device/getImage/**"/>
			<mvc:exclude-mapping path="/images/**"/>
			<mvc:exclude-mapping path="/static/**"/>
			<mvc:exclude-mapping path="/assets/**"/>
			<mvc:exclude-mapping path="/*.html"/>
			<bean class="com.anbao.utils.AuthInterceptor"/>
		</mvc:interceptor>
		<!-- 设备端 API Key 认证 -->
		<mvc:interceptor>
			<mvc:mapping path="/normalup/**"/>
			<mvc:mapping path="/upState/**"/>
			<mvc:mapping path="/uploadVideo/**"/>
			<mvc:mapping path="/addequipment/**"/>
			<mvc:mapping path="/device/getImage/**"/>
			<bean class="com.anbao.utils.ApiKeyInterceptor"/>
		</mvc:interceptor>
	</mvc:interceptors>

	<!-- CORS 配置 - 限制允许的来源 -->
	<mvc:cors>
		<mvc:mapping path="/**"
					 allowed-origins="${CORS_ALLOWED_ORIGINS:http://localhost:8080,http://localhost:3000}"
					 allowed-methods="GET,POST,PUT,DELETE,OPTIONS"
					 allowed-headers="Content-Type,Authorization,X-Requested-With,X-CSRF-TOKEN,X-API-KEY"
					 allow-credentials="true"
					 max-age="3600"/>
	</mvc:cors>

	<mvc:resources location="/images/" mapping="/images/**"></mvc:resources>
	<mvc:resources location="/static/" mapping="/static/**"></mvc:resources>
	<mvc:resources location="/assets/" mapping="/assets/**"></mvc:resources>
	<mvc:resources location="/" mapping="/*.html"></mvc:resources>
	
	<bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/" />
		<property name="suffix" value=".jsp" />
	</bean>
	<!-- 激活annotation功能 -->
	<context:annotation-config />
	<!-- 激活annotation功能 -->
	<context:spring-configured />
</beans>